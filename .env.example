# Tempo AI API Configuration

# Base URL (Optional, defaults to https://api.jointempo.ai/api/v1)
# TEMPOAI_API_BASE_URL=https://api.jointempo.ai/api/v1

# Required: Your Tempo AI API Key
API_KEY=your_tempoai_api_key_here

# The API_KEY will be used as defaults for all API calls
# when these parameters are not explicitly provided to the tool functions.
